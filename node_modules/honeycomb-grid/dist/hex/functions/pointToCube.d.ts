import { HexSettings, Point } from '../types';
/**
 * @category Hex
 */
export declare const pointToCube: ({ dimensions: { xRadius, yRadius }, origin, orientation }: Pick<HexSettings, 'dimensions' | 'origin' | 'orientation'>, { x, y }: Point) => import("../types").CubeCoordinates;
