import { CubeCoordinates, HexOffset, HexSettings, OffsetCoordinates } from '../types';
/**
 * @hidden
 */
export declare const offsetToCubePointy: (col: number, row: number, offset: HexOffset) => CubeCoordinates;
/**
 * @hidden
 */
export declare const offsetToCubeFlat: (col: number, row: number, offset: HexOffset) => CubeCoordinates;
/**
 * @category Hex
 */
export declare const offsetToCube: ({ offset, orientation }: Pick<HexSettings, 'offset' | 'orientation'>, { col, row }: OffsetCoordinates) => CubeCoordinates;
